type Query {
  app(input: AppInput): [App]!
}

input AppInput {
  "클라이언트 ID"
  clientId: ID
}

type App {
  "이름"
  name: String!
  "설명"
  desc: String
  "클라이언트 ID"
  clientId: ID!
  "필수 제공 항목"
  requiredScope: [String]
  "선택 제공 항목"
  optionalScope: [String]
}
