type Mutation {
  "앱 추가하기"
  createApp(input: CreateAppInput!): CreateApp!
}

input CreateAppInput {
  "이름"
  name: String!
  "설명"
  desc: String
}

type CreateApp {
  "이름"
  name: String!
  "클라이언트 ID"
  clientId: ID!
}
